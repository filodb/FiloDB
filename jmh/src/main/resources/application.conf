# Make filodb-defaults.conf visible to libraries (e.g. Kamon) that call
# ConfigFactory.load() directly and therefore only see application.conf,
# not the named config file that FiloDB benchmarks load explicitly.
include "filodb-defaults.conf"
